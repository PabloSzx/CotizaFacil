schema: http://localhost:4000/api/graphql
generates:
  typings/graphql.d.ts:
    - typescript
plugins:
  - typescript
config:
  scalars:
    DateTime: Date
    EmailAddress: string
    JSONObject: Record<string,any>
