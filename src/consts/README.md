# Constants used across the web application, both the client and server.

They might be either completly constants or defined through simple conditions, for example, **process.env.NODE_ENV** environment variable.
